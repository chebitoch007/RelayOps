/** @type {import('next').NextConfig} */
const nextConfig = {
  // TypeScript strict mode is enforced — never ignore build errors.
  // Hiding type errors in production leads to silent runtime failures.
  // Fix type errors at the source; do not suppress them here.
};

module.exports = nextConfig;